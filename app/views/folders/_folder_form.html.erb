<%= form_for(@folder) do |f| %>
  <%= f.label 'Name', class: 'font-weight-bold' %>
  <%= f.text_field :name, class: 'input' %>
  
  <%= f.submit 'Save', class: 'button save-btn' %>
  <%= link_to 'Cancel', folders_path, class: 'button cancel-btn cancel-link' %>
<% end %>
